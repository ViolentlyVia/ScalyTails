<UserControl x:Class="ScalyTails.Views.LogsPage"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             xmlns:converters="clr-namespace:ScalyTails.Converters">

    <UserControl.Resources>
        <converters:BoolToVisibilityConverter x:Key="VisibleWhenFalse" Invert="True"/>
        <converters:InverseBoolConverter x:Key="InverseBool"/>
    </UserControl.Resources>

    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Grid Grid.Row="0" Margin="8,8,8,12">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <StackPanel Grid.Column="0" Orientation="Horizontal">
                <materialDesign:PackIcon Kind="FormatListBulleted" Width="24" Height="24" VerticalAlignment="Center"
                                         Margin="0,0,10,0" Foreground="{DynamicResource PrimaryHueMidBrush}"/>
                <TextBlock Text="Network Logs" Style="{StaticResource MaterialDesignHeadline6TextBlock}" VerticalAlignment="Center"/>
                <TextBlock Text="{Binding StatusMessage}"
                           Style="{StaticResource MaterialDesignCaptionTextBlock}"
                           Foreground="{DynamicResource MaterialDesignBodyLight}"
                           VerticalAlignment="Center" Margin="12,0,0,0"/>
            </StackPanel>
            <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center">
                <TextBlock Text="Last " VerticalAlignment="Center" Margin="0,0,4,0"
                           Style="{StaticResource MaterialDesignBody2TextBlock}"/>
                <ComboBox ItemsSource="{Binding HourOptions}"
                          SelectedItem="{Binding HoursBack}"
                          Width="64" Margin="0,0,4,0" VerticalAlignment="Center"/>
                <TextBlock Text="hours" VerticalAlignment="Center" Margin="0,0,12,0"
                           Style="{StaticResource MaterialDesignBody2TextBlock}"/>
                <Button Command="{Binding RefreshCommand}"
                        Style="{StaticResource MaterialDesignRaisedButton}" Padding="14,6"
                        IsEnabled="{Binding IsBusy, Converter={StaticResource InverseBool}}">
                    <StackPanel Orientation="Horizontal">
                        <materialDesign:PackIcon Kind="Refresh" Margin="0,0,4,0" VerticalAlignment="Center"/>
                        <TextBlock Text="Load"/>
                    </StackPanel>
                </Button>
            </StackPanel>
        </Grid>

        <materialDesign:Card Background="#FFB71C1C" Grid.Row="1" Margin="8,0,8,12" Padding="14"
                              Visibility="{Binding HasApiKey, Converter={StaticResource VisibleWhenFalse}}">
            <StackPanel Orientation="Horizontal">
                <materialDesign:PackIcon Kind="AlertCircle" Foreground="White" Width="20" Height="20"
                                         VerticalAlignment="Center" Margin="0,0,10,0"/>
                <TextBlock Text="API key required. Go to Settings to add one." Foreground="White"
                           Style="{StaticResource MaterialDesignBody2TextBlock}" VerticalAlignment="Center"/>
            </StackPanel>
        </materialDesign:Card>

        <DataGrid Grid.Row="2"
                  ItemsSource="{Binding Logs}"
                  AutoGenerateColumns="False" IsReadOnly="True"
                  CanUserSortColumns="True" SelectionMode="Single"
                  materialDesign:DataGridAssist.CellPadding="10 6 10 6"
                  materialDesign:DataGridAssist.ColumnHeaderPadding="10 6 10 6"
                  Style="{StaticResource MaterialDesignDataGrid}">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Time"
                                    Binding="{Binding Logged, StringFormat='{}{0:yyyy-MM-dd HH:mm:ss}'}"
                                    Width="155" FontFamily="Consolas" FontSize="11"/>
                <DataGridTextColumn Header="Type" Binding="{Binding Type}" Width="80"/>
                <DataGridTextColumn Header="Source" Binding="{Binding TrafficSrc}"
                                    Width="*" FontFamily="Consolas" FontSize="11"/>
                <DataGridTextColumn Header="Destination" Binding="{Binding TrafficDst}"
                                    Width="*" FontFamily="Consolas" FontSize="11"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>
